## 0. 제일 처음

### Step 0-1. 문서 1 (1.md)

* **언제:** 완전 맨 처음 1회.
* **어디까지:**

  * `## 1. 이 문서의 역할`
  * `## 2. 최종 목표: 어떤 기능을 만들 것인가`
  * `## 3. 전체 아키텍처 한 번에 보기`
  * `## 4. Stage별 로드맵 개요`
  * `## 5~7 Stage 1/2/3 개요`까지 그냥 쭉 읽기
* 목적: 전체 지도만 머리에 넣는 용도. 암기할 필요 없음.

---

## 1. Stage 1 준비 (JS/TS + React 기본기)

### Step 1-1. 문서 2 1차 읽기 (JS/TS 위주)

**문서 2 (2.md)**

* **언제:** 첫 코딩 들어가기 전에.

* **어디까지 읽냐:**
  1차는 여기까지만:

  * `## 0. 전제 / 목표`
  * `## 1. JavaScript vs C – 큰 틀 차이`
  * `## 2. JavaScript에서 조심해야 할 것들`
  * `## 3. 비동기 모델: 이벤트 루프, Promise, async/await`
  * `## 4. TypeScript 기초`
  * * 네가 추가한 **JS/TS 보완 섹션** 전체
      (구조 분해, 스프레드, map/filter/find, optional/null/undefined, narrowing, class, `as`, 에러 타입 등)

* 이 단계 목표:

  * TS 함수/인터페이스/유니온 정도 쓸 수 있고
  * `async/await` + `try/catch`가 몸에 조금 익는 수준

**아직 안 읽는 부분 (2차로 미룸):**

* `## 5. Node.js 개요`
* `## 6. Node + TypeScript 프로젝트 골격`
* `## 7. Express로 REST API 만들기`
* `## 8. 간단한 도메인 타입 + 인메모리 REST 예제`
* `## 9. (미리 맛보기) React에서 API 호출 한 번만`
* `## 10. Stage 1 체크리스트 (JS/TS + Node 관점)`

이 네 부분은 **곧바로 실습할 때 참고**할 거라, 그 타이밍에 읽는 게 더 잘 박힌다.

---

### Step 1-2. 문서 3 1차 읽기 (React 기초)

**문서 3 (3.md)**

* **언제:** 문서 2 Step 1-1 끝나고, Node 만지기 전에.
* **어디까지 읽냐 (1차):**

  * `## 0. 전제 / 목표`
  * `## 1. 전체 구조 (프론트 기준)`
  * `## 2. React 프로젝트 기본 (Vite 기준)`
  * `## 3. 타입 정의 / API 모듈 분리`
  * * React 보완 섹션 중:

    - JSX 기본 / JSX vs HTML
    - `style` 객체
    - 조건부 렌더링 (`if return`, `&&` 패턴)

**아직 안 읽는 부분 (2차로 미룸):**

* `## 4. 업로드 상태 관리 컴포넌트 설계`
* `## 5. 업로드 결과 표시 컴포넌트`
* `## 6. 전체 App 구성`
* `## 7. 선택: 업로드 이후 리스트 렌더링`
* `## 8. React에서 상태/렌더링 모델을 C 관점에서 정리`
* `## 9. Stage 1·2 프론트 측 체크리스트`
* * 고급 보완 섹션:

  - `useRef`, `forwardRef`, `useImperativeHandle`, `useEffect` cleanup, `useCallback`, 이벤트 타입 등

이건 **v1.0 들어갈 때 같이 본다.**

---

## 2. Stage 1 미니 실습 (v1.0 전에 손 풀기)

### Step 2-1. 문서 2 2차 읽기 (Node/Express 부분)

**문서 2 (2.md)**

* **언제:** `/ping`, `/echo`, `/videos` 같은 작은 서버 직접 만들기 직전.
* **이번에 추가로 읽을 부분:**

  * `## 5. Node.js 개요`
  * `## 6. Node + TypeScript 프로젝트 골격`
  * `## 7. Express로 REST API 만들기`
  * `## 8. 간단한 도메인 타입 + 인메모리 REST 예제`
  * (9~10은 Stage 1 끝날 때 체크용으로 둬도 됨)

### Step 2-2. 실제로 하는 실습

* 백엔드:

  * `GET /ping` (고정 문자열 혹은 JSON)
  * `POST /echo` (받은 JSON 그대로 돌려주기)
  * `GET /videos` (인메모리 배열 기반 간단 리스트)
* 프론트:

  * React 앱에서 `/ping` 한번 호출해서 화면에 보여주기

이때까지는 **v1.0 설계 일지 안 봐도 됨**.
그냥 문서 2·3 예제로 충분.

---

## 3. v1.0-basic-infrastructure 들어갈 때

### Step 3-1. v1.0 설계 일지 읽기

* `1.0-basic-infrastructure.md`

  * 전체 한 번 읽고, 특히:

    * “지금 버전에서 하는 것 / 안 하는 것”
    * 백엔드 API 스펙
    * 프론트 컴포넌트 구조

### Step 3-2. 문서 3 2차 읽기 (업로드 흐름)

**문서 3 (3.md)**

* **언제:** v1.0 구현 시작 직전.
* **이번에 추가로 읽을 부분:**

  * `## 4. 업로드 상태 관리 컴포넌트 설계`
  * `## 5. 업로드 결과 표시 컴포넌트`
  * `## 6. 전체 App 구성`
* * React 보완 섹션에서:

  - `useState` / `useEffect` 이미 썼다면 복습 정도
  - 폼/이벤트 타입 (`ChangeEvent`, `FormEvent`) 부분

이 세 섹션은 사실상 v1.0 프론트의 청사진이라,
**읽으면서 바로 구현 들어가는 느낌으로 보면 된다.**

### Step 3-3. v1.0 구현

* 백엔드:

  * 이미 만든 `/ping`, `/videos` 기반으로 v1.0 요구사항 맞춰 정리
* 프론트:

  * 문서 3의 4~6장 구조를 거의 그대로 따라 가면서,
    설계 일지에 맞게 컴포넌트/타입 이름만 맞춰가는 식

---

## 4. v1.1, v1.2, v1.3 (FFmpeg CLI + 상태 복잡도 증가)

### Step 4-1. v1.1 들어가기 전에

**문서 2 (2.md)**

* `## 9. (미리 맛보기) React에서 API 호출 한 번만`
* `## 10. Stage 1 체크리스트 (JS/TS + Node 관점)`

체크리스트 보면서 “Stage 1에서 빠진 구멍 있나” 확인.
CLI/파일 관련 내용은 v1.1 설계 일지 + fluent-ffmpeg/child_process 설명에서 더 자세히 다루면 됨.

### Step 4-2. React 고급 패턴은 여기서

**문서 3 + 보완 섹션**

* **언제:** v1.2, v1.3에서 재생 제어/타임라인 넣기 직전.
* **읽을 부분:**

  * 보완 섹션:

    * `useRef`
    * `forwardRef` + `useImperativeHandle`
    * `useEffect` cleanup 패턴
    * `useCallback`
    * 이벤트 타입 (선택)
  * 문서 3의

    * `## 7. 선택: 업로드 이후 리스트 렌더링` (필요하면)
    * `## 8. React에서 상태/렌더링 모델을 C 관점에서 정리`
    * `## 9. Stage 1·2 프론트 측 체크리스트`

이걸 v1.2, v1.3 구현하면서 필요할 때 펼쳐보면 된다.

---

## 5. Stage 3 (C++ / FFmpeg C API) 시점에서 문서 4

### Step 5-1. v2.0-native-addon-setup 들어가기 직전

**문서 4 (4.md)** + C++/CMake 보완 섹션

* **언제:** v1.3까지 끝나고, CLI 기반으로는 서비스가 돌아가는 상태에서.
* **한 번에 읽는 범위 (권장):**

  * `## 0. 전제 / 목표`
  * `## 1. FFmpeg C API를 C++에서 쓰기 위한 기본 셋업`
  * `## 2. C vs 현대 C++ – 여기서 필요한 부분만 정리`
  * `## 3. 예제 1 – C++에서 영상 메타데이터 읽기`
  * `## 4. CLI 프로그램: video_info`
  * `## 5. CMake 빌드 설정 예시`
  * `## 6. Node에서 C++ CLI를 호출해서 쓰기`
  * `## 7. 네이티브 애드온 (선택)`
  * `## 8. 정리 / Stage 3 체크리스트`
* * C++/CMake 보완 섹션 전체:

  - 참조, RAII, `string`/`vector`, `unique_ptr`
  - `auto` + range-for + `std::map`
  - `using` 타입 별칭, `std::move`
  - C++ 예외 + N-API 에러 브리지
  - CMake 기본
  - (선택) FFmpeg 버전 분기, 벤치마크 패턴

**그 다음 시퀀스:**

* v2.0-native-addon-setup
* v2.1-thumbnail-extraction
* v2.2-metadata-analysis

이 때는 문서 2·3은 거의 참고만 하고, **문서 4 + v2.x 설계 일지**를 메인으로 보면 된다.

---

## 최종 타임라인 요약 (문서 기준만)

1. **시작 전**

   * 1.md 전체 1회

2. **Stage 1 준비**

   * 2.md: `0~4` + JS/TS 보완
   * 3.md: `0~3` + React JSX/조건부 렌더링 보완

3. **Stage 1 미니 실습(/ping, /echo, /videos)**

   * 2.md: `5~8` 추가로 읽으면서 바로 실습

4. **v1.0**

   * 1.0 설계 일지 정독
   * 3.md: `4~6` + 폼/이벤트 타입 보완 보면서 구현

5. **v1.1, v1.2, v1.3**

   * 2.md: `9~10` 체크
   * 3.md: `7~9` + `useRef/forwardRef/useEffect/useCallback` 보완 활용

6. **v2.0, v2.1, v2.2**

   * 4.md: `0~8` 전체 + C++/CMake 보완
   * 이후 Stage 3 구현

